% Usage: \usepackage[options]{spaper}
%        \usepackage[options]{knitrl}
%        \begin{document}
%        . . .
%        \printbibliography[title={References}]}
%        \end{document}
%
% spaper
% option     Default     Meaning
% ------     -------     -------
% margin     1.0in       Margins with geometry package, all sides
% bib        (none)      Full path and name of BiBTeX .bib file

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{spaper}[2015/10/30 Style for manuscripts incorporating knitr and author comments]

\RequirePackage{xkeyval,relsize,biblatex,lscape,fancyhdr}

\DeclareOptionX{margin}{\def\margin{#1}}
\DeclareOptionX{bib}{\addbibresource{#1}}
\ExecuteOptionsX{margin=1in}
\ProcessOptionsX\relax

\usepackage[margin=\margin]{geometry}
\pagestyle{fancy}

\newcommand{\code}[1]{{\smaller\texttt{#1}}}
\def\R{{\normalfont\textsf{R}}{}}

\RequirePackage{tcolorbox}
\definecolor{lemonchiffon}{rgb}{1.0, 0.98, 0.8}
\newtcbox{\ovbox}{colback=lemonchiffon,boxrule=0pt,arc=5pt,
  boxsep=0pt,left=3pt,right=3pt,top=3pt,bottom=3pt}

\newcommand{\com}[2]{\centerline{\ovbox{\parbox{.9\textwidth}{\smaller#2}}}%
\marginpar{\textcolor{blue}{\smaller[3]#1}}}
\newcommand{\snote}[1]{\marginpar{\textcolor{blue}{\smaller[3]#1}}}
\newcommand{\ignorecom}{%
 \renewcommand{\com}[2]{}%
 \renewcommand{\snote}[1]{}}

\endinput

sudo cp ~/doc/latex/texinput/spaper.sty /usr/share/R/share/texmf/.
sudo mktexlsr

To update on wiki:
cp -p spaper.sty /tmp
browser http://biostat.mc.vanderbilt.edu/wiki/bin/attach/Main/LaTeXCollab?filename=spaper.sty;revInfo=1
